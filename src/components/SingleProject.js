import React from 'react';
import { Typography } from '@material-ui/core';

const SingleProject = (props) => {
  return (
    <div>
      <Typography variant="h3">Single Project</Typography>
    </div>
  );
};

export default SingleProject;
